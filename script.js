const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});reveals.forEach(el=>observer.observe(el));const stage=document.querySelector('.product-stage');const bottle=document.querySelector('.hero .bottle');if(stage&&bottle&&matchMedia('(min-width:901px)').matches){stage.addEventListener('mousemove',e=>{const r=stage.getBoundingClientRect();const x=(e.clientX-r.left-r.width/2)/r.width;const y=(e.clientY-r.top-r.height/2)/r.height;bottle.style.marginLeft=`${x*18}px`;bottle.style.marginTop=`${y*12}px`});stage.addEventListener('mouseleave',()=>{bottle.style.marginLeft='0';bottle.style.marginTop='0'})}document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');if(id==='#')return;const el=document.querySelector(id);if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth',block:'start'})}}));